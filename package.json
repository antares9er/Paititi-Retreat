{
  "name": "paititi-retreat",
  "version": "1.0.0",
  "description": "Paititi Retreats website with a small admin backend (Cloudflare Worker with static assets)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 3000",
    "deploy": "wrangler deploy",
    "set-password": "node scripts/set-password.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}

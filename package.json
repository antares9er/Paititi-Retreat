{
  "name": "paititi-retreat",
  "version": "1.0.0",
  "description": "Paititi Retreats website with a small admin backend (Cloudflare Pages Functions)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev . --port 3000 --kv CONTENT --r2 UPLOADS",
    "set-password": "node scripts/set-password.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
